{
  "name": "@buenro/root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "start:dev": "npm run start:dev --workspace=@buenro/api & npm run start:dev --workspace=@buenro/ingestion",
    "start:api": "npm run start:dev --workspace=@buenro/api",
    "start:ingestion": "npm run start:dev --workspace=@buenro/ingestion",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "author": "",
  "license": "ISC",
  "description": "Buenro Test Monorepo"
}
