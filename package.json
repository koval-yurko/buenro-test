{
  "name": "@buenro/root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "build:common": "npm run build --workspace=@buenro/common",
    "build:api": "npm run build:common && npm run build --workspace=@buenro/api",
    "build:ingestion": "npm run build:common && npm run build --workspace=@buenro/ingestion",
    "start:dev": "npm run build:common && npm run start:dev --workspace=@buenro/api & npm run start:dev --workspace=@buenro/ingestion",
    "start:api": "npm run build:common && npm run start:dev --workspace=@buenro/api",
    "start:ingestion": "npm run build:common && npm run start:dev --workspace=@buenro/ingestion",
    "test": "npm run test --workspaces --if-present",
    "test:cov": "npm run test:cov --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "format": "npm run format --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "author": "",
  "license": "ISC",
  "description": "Buenro Test Monorepo"
}
